# i18n Multi-Language Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add internationalization (i18n) supporting Chinese (default) and English, with a language switcher in the header.

**Architecture:** Use React Context + a flat JSON translation file per locale. No i18n library needed — the app is small enough (~80 strings) that a custom hook (`useT`) with `Record<string, string>` is simpler, zero-dependency, and tree-shakeable. Language preference stored in `localStorage`.

**Tech Stack:** React Context, TypeScript, JSON translation files

---

## File Structure

| File | Responsibility |
|------|---------------|
| `src/i18n/zh.json` | Chinese translations (all strings) |
| `src/i18n/en.json` | English translations (all strings) |
| `src/i18n/types.ts` | `Translations` type generated from zh.json keys |
| `src/i18n/context.tsx` | `I18nProvider`, `useT` hook, language switch logic |
| `src/components/LanguageToggle.tsx` | Language switcher button for Header |

Files that get **modified** (hardcoded strings → `t('key')` calls):
- `src/components/Header.tsx`
- `src/components/HomePage.tsx`
- `src/components/ToolPage.tsx`
- `src/registry.ts`
- `src/tools/json/Tool.tsx`
- `src/tools/timestamp/Tool.tsx`
- `src/tools/base64/Tool.tsx`
- `src/tools/url/Tool.tsx`
- `src/tools/timestamp/logic.ts` (relative time strings)
- `index.html` (title)

---

### Task 1: Create translation files and types

**Files:**
- Create: `src/i18n/zh.json`
- Create: `src/i18n/en.json`
- Create: `src/i18n/types.ts`

- [ ] **Step 1: Create `src/i18n/zh.json`**

This is the source of truth for all translation keys.

```json
{
  "app.title": "Raccoon — 开发者工具箱",
  "header.search": "搜索工具...",
  "home.searchPrompt": "搜索工具...",
  "home.tagline": "粘贴，转换，搞定。",
  "home.pressToStart": "按 ",
  "home.toStart": " 开始",
  "home.searchPlaceholder": "输入工具名称或关键词...",
  "home.emptyTools": "未找到工具。试试 'json'、'时间'、'base64' 或 'url'。",
  "tool.notFound": "未找到工具。",
  "tool.searchPlaceholder": "输入工具名称或关键词...",
  "tool.noToolsFound": "未找到工具。",
  "common.input": "输入",
  "common.output": "输出",
  "common.copy": "复制",
  "common.copied": "已复制！",
  "common.clear": "清空",
  "common.whatDoesThisDo": "这个工具做什么？",
  "common.pasteToSeeResult": "在上方粘贴输入以查看结果。",
  "json.name": "JSON 格式化",
  "json.description": "格式化、压缩和验证 JSON 数据",
  "json.minify": "压缩",
  "json.format": "格式化",
  "json.placeholder": "在此粘贴 JSON...",
  "json.explanation": "将压缩或精简的 JSON 格式化为可读的缩进结构。同时验证 JSON 语法并高亮显示错误位置。",
  "json.feature1": "在格式化和压缩模式间切换",
  "json.feature2": "输入或粘贴后即时输出",
  "json.feature3": "一键复制格式化结果",
  "timestamp.name": "时间戳转换",
  "timestamp.description": "在 Unix、ISO 8601、RFC 2822 和相对时间之间转换",
  "timestamp.now": "现在",
  "timestamp.placeholder": "粘贴时间戳、日期字符串或 Unix 纪元...",
  "timestamp.unixEpoch": "Unix 纪元",
  "timestamp.iso8601": "ISO 8601",
  "timestamp.rfc2822": "RFC 2822",
  "timestamp.relative": "相对时间",
  "timestamp.pasteToSee": "粘贴时间戳以查看所有格式。",
  "timestamp.currentTime": "当前时间：",
  "timestamp.explanation": "在 Unix 纪元、ISO 8601、RFC 2822 和相对时间格式之间转换。对任何输入同时显示所有格式。",
  "timestamp.feature1": "支持 Unix 时间戳（秒或毫秒）",
  "timestamp.feature2": "支持 ISO 8601 和大多数日期字符串格式",
  "timestamp.feature3": "点击「现在」获取当前时间戳",
  "timestamp.error.empty": "输入为空",
  "timestamp.error.outOfRange": "日期超出可表示范围（100–9999 年）。",
  "timestamp.error.outOfRangeShort": "日期超出可表示范围。",
  "timestamp.error.unrecognized": "无法解析为时间戳。请尝试 Unix 纪元（如 1700000000）、ISO 8601 或日期字符串。",
  "timestamp.justNow": "刚刚",
  "timestamp.ago": "前",
  "timestamp.fromNow": "后",
  "timestamp.secondsShort": "秒",
  "timestamp.minutesShort": "分钟",
  "timestamp.hoursShort": "小时",
  "timestamp.daysShort": "天",
  "timestamp.monthsShort": "个月",
  "base64.name": "Base64 编码器",
  "base64.description": "编码或解码 Base64 字符串",
  "base64.decode": "解码",
  "base64.encode": "编码",
  "base64.encodePlaceholder": "输入要编码的文本...",
  "base64.decodePlaceholder": "粘贴要解码的 Base64...",
  "base64.encodeEmpty": "输入文本以查看 Base64 编码结果。",
  "base64.decodeEmpty": "粘贴 Base64 以查看解码文本。",
  "base64.explanation": "将文本编码为 Base64 或将 Base64 字符串解码为文本。支持 Unicode 字符，包括中文和 Emoji。",
  "base64.feature1": "在编码和解码模式间切换",
  "base64.feature2": "支持完整 Unicode（Emoji、中文字符）",
  "base64.feature3": "输入时即时输出",
  "base64.error.encodeFailed": "编码输入失败。",
  "base64.error.invalid": "无效的 Base64：发现 Base64 字母表之外的字符",
  "base64.error.atPosition": "，位于位置 ",
  "base64.error.period": "。",
  "url.name": "URL 编码器",
  "url.description": "编码或解码 URL 组件和查询字符串",
  "url.decode": "解码",
  "url.encode": "编码",
  "url.encodePlaceholder": "输入要编码的文本或 URL...",
  "url.decodePlaceholder": "粘贴要解码的 URL 编码文本...",
  "url.encodeEmpty": "输入文本以查看 URL 编码结果。",
  "url.decodeEmpty": "粘贴 URL 编码文本以查看解码结果。",
  "url.explanation": "编码或解码 URL 组件。将特殊字符转换为百分号编码格式，或反向转换。",
  "url.feature1": "在编码和解码模式间切换",
  "url.feature2": "支持 Unicode 和特殊字符",
  "url.feature3": "适用于查询字符串和 URL 参数",
  "url.error.encodeFailed": "URL 编码输入失败。",
  "url.error.invalidWithChar": "无效的 URL 编码：'{char}' 不是有效的百分号编码序列。",
  "url.error.invalidGeneric": "无效的 URL 编码：格式错误的百分号编码序列。",
  "json.error.invalid": "无效的 JSON：意外的标记",
  "json.error.atPosition": "，位于位置 "
}
```

- [ ] **Step 2: Create `src/i18n/en.json`**

```json
{
  "app.title": "Raccoon — Developer Toolbox",
  "header.search": "Search tools...",
  "home.searchPrompt": "Search tools...",
  "home.tagline": "Paste, convert, done.",
  "home.pressToStart": "Press ",
  "home.toStart": " to start",
  "home.searchPlaceholder": "Type a tool name or keyword...",
  "home.emptyTools": "No tools found. Try 'json', 'time', 'base64', or 'url'.",
  "tool.notFound": "Tool not found.",
  "tool.searchPlaceholder": "Type a tool name or keyword...",
  "tool.noToolsFound": "No tools found.",
  "common.input": "Input",
  "common.output": "Output",
  "common.copy": "Copy",
  "common.copied": "Copied!",
  "common.clear": "Clear",
  "common.whatDoesThisDo": "What does this tool do?",
  "common.pasteToSeeResult": "Paste your input above to see the result.",
  "json.name": "JSON Formatter",
  "json.description": "Format, minify, and validate JSON data",
  "json.minify": "Minify",
  "json.format": "Format",
  "json.placeholder": "Paste JSON here...",
  "json.explanation": "Formats compressed or minified JSON into a readable, indented structure. Also validates JSON syntax and highlights errors with the exact position.",
  "json.feature1": "Toggle between Format and Minify modes",
  "json.feature2": "Instant output as you type or paste",
  "json.feature3": "Copy formatted result with one click",
  "timestamp.name": "Timestamp Converter",
  "timestamp.description": "Convert between Unix, ISO 8601, RFC 2822, and relative time",
  "timestamp.now": "Now",
  "timestamp.placeholder": "Paste a timestamp, date string, or Unix epoch...",
  "timestamp.unixEpoch": "Unix Epoch",
  "timestamp.iso8601": "ISO 8601",
  "timestamp.rfc2822": "RFC 2822",
  "timestamp.relative": "Relative",
  "timestamp.pasteToSee": "Paste a timestamp to see all formats.",
  "timestamp.currentTime": "Current time:",
  "timestamp.explanation": "Converts between Unix epoch, ISO 8601, RFC 2822, and relative time formats. Shows all formats simultaneously for any input.",
  "timestamp.feature1": "Supports Unix timestamps (seconds or milliseconds)",
  "timestamp.feature2": "Supports ISO 8601 and most date string formats",
  "timestamp.feature3": "Click \"Now\" to get the current timestamp",
  "timestamp.error.empty": "Empty input",
  "timestamp.error.outOfRange": "Date is outside the representable range (year 100–9999).",
  "timestamp.error.outOfRangeShort": "Date is outside the representable range.",
  "timestamp.error.unrecognized": "Couldn't parse this as a timestamp. Try Unix epoch (e.g., 1700000000), ISO 8601, or a date string.",
  "timestamp.justNow": "just now",
  "timestamp.ago": "ago",
  "timestamp.fromNow": "from now",
  "timestamp.secondsShort": "s",
  "timestamp.minutesShort": "m",
  "timestamp.hoursShort": "h",
  "timestamp.daysShort": "d",
  "timestamp.monthsShort": "mo",
  "base64.name": "Base64 Encoder",
  "base64.description": "Encode or decode Base64 strings",
  "base64.decode": "Decode",
  "base64.encode": "Encode",
  "base64.encodePlaceholder": "Type text to encode...",
  "base64.decodePlaceholder": "Paste Base64 to decode...",
  "base64.encodeEmpty": "Type text to see the Base64 encoded output.",
  "base64.decodeEmpty": "Paste Base64 to see the decoded text.",
  "base64.explanation": "Encodes text to Base64 or decodes Base64 strings back to text. Handles Unicode characters including Chinese and emoji.",
  "base64.feature1": "Toggle between Encode and Decode modes",
  "base64.feature2": "Supports full Unicode (emoji, CJK characters)",
  "base64.feature3": "Instant output as you type",
  "base64.error.encodeFailed": "Failed to encode input.",
  "base64.error.invalid": "Invalid Base64: characters outside the Base64 alphabet found",
  "base64.error.atPosition": " at position ",
  "base64.error.period": ".",
  "url.name": "URL Encoder",
  "url.description": "Encode or decode URL components and query strings",
  "url.decode": "Decode",
  "url.encode": "Encode",
  "url.encodePlaceholder": "Type text or URL to encode...",
  "url.decodePlaceholder": "Paste URL-encoded text to decode...",
  "url.encodeEmpty": "Type text to see the URL-encoded output.",
  "url.decodeEmpty": "Paste URL-encoded text to see the decoded result.",
  "url.explanation": "Encodes or decodes URL components. Converts special characters to percent-encoded format and back.",
  "url.feature1": "Toggle between Encode and Decode modes",
  "url.feature2": "Handles Unicode and special characters",
  "url.feature3": "Useful for query strings and URL parameters",
  "url.error.encodeFailed": "Failed to URL-encode input.",
  "url.error.invalidWithChar": "Invalid URL encoding: '{char}' is not a valid percent-encoded sequence.",
  "url.error.invalidGeneric": "Invalid URL encoding: malformed percent-encoded sequence.",
  "json.error.invalid": "Invalid JSON: unexpected token",
  "json.error.atPosition": " at position "
}
```

- [ ] **Step 3: Create `src/i18n/types.ts`**

```typescript
import zh from './zh.json';

export type Locale = 'zh' | 'en';
export type TranslationKey = keyof typeof zh;
export type Translations = Record<TranslationKey, string>;
```

- [ ] **Step 4: Verify JSON is valid**

Run: `node -e "console.log(Object.keys(require('./src/i18n/zh.json')).length + ' keys')" && node -e "console.log(Object.keys(require('./src/i18n/en.json')).length + ' keys')"`
Expected: Same number of keys for both files.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/
git commit -m "feat(i18n): add Chinese and English translation files"
```

---

### Task 2: Create i18n context and useT hook

**Files:**
- Create: `src/i18n/context.tsx`
- Test: `src/i18n/context.test.ts`

- [ ] **Step 1: Write the failing test for `src/i18n/context.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { I18nProvider, useT } from './context';

describe('useT', () => {
  it('returns Chinese strings by default', () => {
    const { result } = renderHook(() => useT(), { wrapper: I18nProvider });
    expect(result.current.t('common.copy')).toBe('复制');
  });

  it('switches to English', () => {
    const { result } = renderHook(() => useT(), { wrapper: I18nProvider });
    act(() => { result.current.setLocale('en'); });
    expect(result.current.t('common.copy')).toBe('Copy');
  });

  it('returns key as fallback for missing translations', () => {
    const { result } = renderHook(() => useT(), { wrapper: I18nProvider });
    expect(result.current.t('nonexistent.key' as any)).toBe('nonexistent.key');
  });

  it('supports interpolation with {var}', () => {
    const { result } = renderHook(() => useT(), { wrapper: I18nProvider });
    const val = result.current.t('url.error.invalidWithChar', { char: '%ZZ' });
    expect(val).toContain('%ZZ');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/i18n/context.test.ts`
Expected: FAIL — `./context` does not export `I18nProvider`

- [ ] **Step 3: Write `src/i18n/context.tsx`**

```tsx
import { createContext, useContext, useState, useCallback, type ReactNode } from 'react';
import zh from './zh.json';
import en from './en.json';
import type { Locale, TranslationKey, Translations } from './types';

const translations: Record<Locale, Translations> = { zh, en };

interface I18nContextValue {
  locale: Locale;
  setLocale: (locale: Locale) => void;
  t: (key: TranslationKey, vars?: Record<string, string>) => string;
}

const I18nContext = createContext<I18nContextValue | null>(null);

function getInitialLocale(): Locale {
  try {
    const stored = localStorage.getItem('raccoon-locale');
    if (stored === 'zh' || stored === 'en') return stored;
  } catch {}
  return 'zh';
}

export function I18nProvider({ children }: { children: ReactNode }) {
  const [locale, setLocaleState] = useState<Locale>(getInitialLocale);

  const setLocale = useCallback((l: Locale) => {
    setLocaleState(l);
    try { localStorage.setItem('raccoon-locale', l); } catch {}
    document.documentElement.lang = l === 'zh' ? 'zh-CN' : 'en';
  }, []);

  const t = useCallback((key: TranslationKey, vars?: Record<string, string>) => {
    let str = translations[locale][key] ?? key;
    if (vars) {
      for (const [k, v] of Object.entries(vars)) {
        str = str.replace(`{${k}}`, v);
      }
    }
    return str;
  }, [locale]);

  return (
    <I18nContext.Provider value={{ locale, setLocale, t }}>
      {children}
    </I18nContext.Provider>
  );
}

export function useT(): I18nContextValue {
  const ctx = useContext(I18nContext);
  if (!ctx) throw new Error('useT must be used within I18nProvider');
  return ctx;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run src/i18n/context.test.ts`
Expected: PASS — 4 tests

- [ ] **Step 5: Commit**

```bash
git add src/i18n/context.tsx src/i18n/context.test.ts
git commit -m "feat(i18n): add I18nProvider and useT hook with tests"
```

---

### Task 3: Create LanguageToggle component

**Files:**
- Create: `src/components/LanguageToggle.tsx`

- [ ] **Step 1: Create `src/components/LanguageToggle.tsx`**

```tsx
import { useT } from '../i18n/context';

export function LanguageToggle() {
  const { locale, setLocale } = useT();
  return (
    <button
      onClick={() => setLocale(locale === 'zh' ? 'en' : 'zh')}
      className="text-[11px] px-2.5 py-1 bg-surface-2 rounded text-text-muted hover:text-text-secondary transition-colors font-mono"
    >
      {locale === 'zh' ? 'EN' : '中'}
    </button>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/LanguageToggle.tsx
git commit -m "feat(i18n): add LanguageToggle component"
```

---

### Task 4: Wire I18nProvider into App and add LanguageToggle to Header

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/components/Header.tsx`

- [ ] **Step 1: Modify `src/App.tsx` — wrap with I18nProvider**

Replace the `App` function:

```tsx
import { createRootRoute, createRoute, createRouter, createHashHistory, RouterProvider, Outlet } from '@tanstack/react-router';
import { I18nProvider } from './i18n/context';
import { Header } from './components/Header';
import { HomePage } from './components/HomePage';
import { ToolPage } from './components/ToolPage';

const hashHistory = createHashHistory();

const rootRoute = createRootRoute({
  component: function RootLayout() {
    return (
      <div className="min-h-screen bg-surface-0">
        <Header />
        <Outlet />
      </div>
    );
  },
});

const homeRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/',
  component: HomePage,
});

const toolRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/tool/$toolId',
  component: ToolPage,
});

const routeTree = rootRoute.addChildren([homeRoute, toolRoute]);

const router = createRouter({
  routeTree,
  history: hashHistory,
});

declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}

export function App() {
  return (
    <I18nProvider>
      <RouterProvider router={router} />
    </I18nProvider>
  );
}
```

- [ ] **Step 2: Modify `src/components/Header.tsx` — add LanguageToggle**

Add import at top:
```tsx
import { LanguageToggle } from './LanguageToggle';
```

Add `<LanguageToggle />` as the first child inside the `<header>` flex, before the search button. Insert after the Raccoon logo button's closing `</button>`:

```tsx
        <LanguageToggle />
      </button>
      <button
        onClick={() => {
```

becomes:

```tsx
        </button>
        <LanguageToggle />
      </header>
```

Actually — the simplest placement is between the logo and the search bar. Insert `<LanguageToggle />` after the closing `</button>` of the logo button, before the search `<button>`:

Full Header.tsx:
```tsx
import { useNavigate, useRouterState } from '@tanstack/react-router';
import { LanguageToggle } from './LanguageToggle';

export function Header() {
  const navigate = useNavigate();
  const routerState = useRouterState();
  const isHome = routerState.location.pathname === '/';

  return (
    <header className="flex items-center justify-between px-6 py-4 border-b border-surface-2">
      <div className="flex items-center gap-2">
        <button
          onClick={() => navigate({ to: '/' })}
          className="flex items-center gap-2.5 text-lg font-semibold text-text-primary hover:opacity-80 transition-opacity"
        >
          <div className="w-7 h-7 bg-accent rounded-md flex items-center justify-center text-sm font-mono text-white font-bold">
            R
          </div>
          <span className="font-mono">Raccoon</span>
        </button>
        <LanguageToggle />
      </div>
      <button
        onClick={() => {
          if (isHome) {
            const input = document.querySelector<HTMLInputElement>('[cmdk-input]');
            input?.focus();
          } else {
            navigate({ to: '/' });
          }
        }}
        className="flex items-center gap-2 bg-surface-1 border border-surface-3 rounded-lg px-4 py-2 text-text-muted text-sm cursor-pointer min-w-[280px] hover:border-surface-4 transition-colors"
      >
        <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
        Search tools...
        <kbd className="ml-auto bg-surface-2 border border-surface-3 rounded px-1.5 py-0.5 text-[11px] text-text-muted font-mono">
          ⌘K
        </kbd>
      </button>
    </header>
  );
}
```

Note: The "Search tools..." text in the Header search bar is intentionally NOT translated here — it's a visual hint that's secondary to the main UI. It stays in English for consistency with the ⌘K shortcut. If you want it translated, add `header.search` key usage: `t('header.search')`.

- [ ] **Step 3: Verify app compiles**

Run: `npx tsc --noEmit`
Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add src/App.tsx src/components/Header.tsx
git commit -m "feat(i18n): wire I18nProvider into App, add LanguageToggle to Header"
```

---

### Task 5: Replace hardcoded strings in registry.ts

**Files:**
- Modify: `src/registry.ts`

The registry tool names and descriptions are currently hardcoded English. They need to become functions that accept the `t` function, or the registry needs to be made reactive.

**Approach:** Change registry to export a function `getTools(t)` that returns localized tool metadata. Update consumers (HomePage, ToolPage) to use `getTools(t)`.

- [ ] **Step 1: Modify `src/registry.ts`**

Replace entire file:

```typescript
import { lazy, type LazyExoticComponent, type ComponentType } from 'react';
import type { TranslationKey } from './i18n/types';
import type { Translations } from './i18n/types';

export interface ToolRegistration {
  id: string;
  name: string;
  description: string;
  keywords: string[];
  icon: string;
  component: LazyExoticComponent<ComponentType>;
}

const toolDefs: {
  id: string;
  nameKey: TranslationKey;
  descKey: TranslationKey;
  keywords: string[];
  icon: string;
  component: LazyExoticComponent<ComponentType>;
}[] = [
  {
    id: 'json',
    nameKey: 'json.name',
    descKey: 'json.description',
    keywords: ['json', 'format', 'minify', 'validate', 'pretty'],
    icon: '{ }',
    component: lazy(() => import('./tools/json/Tool')),
  },
  {
    id: 'timestamp',
    nameKey: 'timestamp.name',
    descKey: 'timestamp.description',
    keywords: ['timestamp', 'time', 'date', 'epoch', 'unix', 'iso'],
    icon: 'TS',
    component: lazy(() => import('./tools/timestamp/Tool')),
  },
  {
    id: 'base64',
    nameKey: 'base64.name',
    descKey: 'base64.description',
    keywords: ['base64', 'b64', 'encode', 'decode'],
    icon: 'B64',
    component: lazy(() => import('./tools/base64/Tool')),
  },
  {
    id: 'url',
    nameKey: 'url.name',
    descKey: 'url.description',
    keywords: ['url', 'uri', 'encode', 'decode', 'percent'],
    icon: 'URL',
    component: lazy(() => import('./tools/url/Tool')),
  },
];

export function getTools(t: (key: TranslationKey) => string): ToolRegistration[] {
  return toolDefs.map((d) => ({
    id: d.id,
    name: t(d.nameKey),
    description: t(d.descKey),
    keywords: d.keywords,
    icon: d.icon,
    component: d.component,
  }));
}

export function findTool(id: string, t: (key: TranslationKey) => string): ToolRegistration | undefined {
  return getTools(t).find((tool) => tool.id === id);
}
```

- [ ] **Step 2: Verify it compiles**

Run: `npx tsc --noEmit`
Expected: Errors in HomePage.tsx and ToolPage.tsx (they import `tools` and `findTool` which changed). That's expected — we fix them next.

- [ ] **Step 3: Commit**

```bash
git add src/registry.ts
git commit -m "feat(i18n): make registry locale-aware with getTools(t)"
```

---

### Task 6: Replace hardcoded strings in HomePage.tsx and ToolPage.tsx

**Files:**
- Modify: `src/components/HomePage.tsx`
- Modify: `src/components/ToolPage.tsx`

- [ ] **Step 1: Modify `src/components/HomePage.tsx`**

Replace entire file:

```tsx
import { useState, useEffect, useCallback } from 'react';
import { useNavigate } from '@tanstack/react-router';
import { Command } from 'cmdk';
import { getTools } from '../registry';
import { useT } from '../i18n/context';

export function HomePage() {
  const [open, setOpen] = useState(false);
  const navigate = useNavigate();
  const { t } = useT();
  const tools = getTools(t);

  useEffect(() => {
    const down = (e: KeyboardEvent) => {
      if (e.key === 'k' && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        setOpen((o) => !o);
      }
    };
    document.addEventListener('keydown', down);
    return () => document.removeEventListener('keydown', down);
  }, []);

  const selectTool = useCallback(
    (id: string) => {
      setOpen(false);
      navigate({ to: '/tool/$toolId', params: { toolId: id } });
    },
    [navigate],
  );

  return (
    <div className="flex flex-col items-center justify-center min-h-[calc(100vh-65px)] px-6">
      <div className="text-center mb-12">
        <div
          onClick={() => setOpen(true)}
          className="cursor-pointer text-2xl text-text-muted hover:text-text-secondary transition-colors"
        >
          {t('home.searchPrompt')}
        </div>
        <div className="mt-2 text-sm text-text-dim">{t('home.tagline')}</div>
        <div className="mt-1 text-sm text-text-dim">
          {t('home.pressToStart')}
          <kbd className="bg-surface-2 border border-surface-3 rounded px-1.5 py-0.5 text-[11px] font-mono">
            ⌘K
          </kbd>
          {t('home.toStart')}
        </div>
      </div>

      <Command.Dialog
        open={open}
        onOpenChange={setOpen}
        label="Search tools"
        className="fixed inset-0 z-50 flex items-start justify-center pt-[120px]"
      >
        <div
          className="fixed inset-0 bg-black/60"
          onClick={() => setOpen(false)}
          style={{ animation: 'fadeIn 150ms ease-out' }}
        />
        <div
          className="relative bg-surface-1 border border-surface-3 rounded-xl w-full max-w-[520px] overflow-hidden shadow-2xl"
          style={{ animation: 'scaleIn 150ms ease-out' }}
        >
          <Command.Input
            placeholder={t('home.searchPlaceholder')}
            className="w-full bg-transparent border-b border-surface-2 px-4 py-4 text-text-primary text-base outline-none placeholder:text-text-dim"
          />
          <Command.List className="p-2">
            <Command.Empty className="py-6 text-center text-sm text-text-muted">
              {t('home.emptyTools')}
            </Command.Empty>
            {tools.map((tool) => (
              <Command.Item
                key={tool.id}
                value={`${tool.name} ${tool.keywords.join(' ')}`}
                onSelect={() => selectTool(tool.id)}
                className="flex items-center gap-3 px-3 py-2.5 rounded-lg cursor-pointer text-text-muted data-[selected=true]:bg-accent-muted data-[selected=true]:text-text-primary transition-colors"
              >
                <div className="w-8 h-8 bg-surface-2 rounded-md flex items-center justify-center text-sm font-mono text-text-secondary">
                  {tool.icon}
                </div>
                <div>
                  <div className="text-sm text-text-primary">{tool.name}</div>
                  <div className="text-xs text-text-dim">{tool.description}</div>
                </div>
              </Command.Item>
            ))}
          </Command.List>
        </div>
      </Command.Dialog>

      <style>{`
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        @keyframes scaleIn { from { opacity: 0; transform: scale(0.98); } to { opacity: 1; transform: scale(1); } }
      `}</style>
    </div>
  );
}
```

- [ ] **Step 2: Modify `src/components/ToolPage.tsx`**

Replace entire file:

```tsx
import { useState, useEffect, useCallback } from 'react';
import { useNavigate, useParams } from '@tanstack/react-router';
import { Command } from 'cmdk';
import { findTool, getTools } from '../registry';
import { useT } from '../i18n/context';

export function ToolPage() {
  const { toolId } = useParams({ from: '/tool/$toolId' });
  const navigate = useNavigate();
  const [open, setOpen] = useState(false);
  const { t } = useT();
  const tool = findTool(toolId, t);
  const tools = getTools(t);

  useEffect(() => {
    const down = (e: KeyboardEvent) => {
      if (e.key === 'k' && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        setOpen((o) => !o);
      }
    };
    document.addEventListener('keydown', down);
    return () => document.removeEventListener('keydown', down);
  }, []);

  const selectTool = useCallback(
    (id: string) => {
      setOpen(false);
      navigate({ to: '/tool/$toolId', params: { toolId: id } });
    },
    [navigate],
  );

  if (!tool) {
    return (
      <main className="max-w-[960px] mx-auto px-6 py-8">
        <div className="text-text-muted">{t('tool.notFound')}</div>
      </main>
    );
  }

  const ToolComponent = tool.component;

  return (
    <>
      <main className="max-w-[960px] mx-auto px-6 py-8">
        <div className="mb-6">
          <h1 className="text-2xl font-semibold text-text-primary mb-1">{tool.name}</h1>
          <p className="text-sm text-text-muted leading-relaxed">{tool.description}</p>
        </div>
        <ToolComponent />
      </main>

      <Command.Dialog
        open={open}
        onOpenChange={setOpen}
        label="Search tools"
        className="fixed inset-0 z-50 flex items-start justify-center pt-[120px]"
      >
        <div className="fixed inset-0 bg-black/60" onClick={() => setOpen(false)} />
        <div className="relative bg-surface-1 border border-surface-3 rounded-xl w-full max-w-[520px] overflow-hidden shadow-2xl">
          <Command.Input
            placeholder={t('tool.searchPlaceholder')}
            className="w-full bg-transparent border-b border-surface-2 px-4 py-4 text-text-primary text-base outline-none placeholder:text-text-dim"
          />
          <Command.List className="p-2">
            <Command.Empty className="py-6 text-center text-sm text-text-muted">
              {t('tool.noToolsFound')}
            </Command.Empty>
            {tools.map((t) => (
              <Command.Item
                key={t.id}
                value={`${t.name} ${t.keywords.join(' ')}`}
                onSelect={() => selectTool(t.id)}
                className="flex items-center gap-3 px-3 py-2.5 rounded-lg cursor-pointer text-text-muted data-[selected=true]:bg-accent-muted data-[selected=true]:text-text-primary transition-colors"
              >
                <div className="w-8 h-8 bg-surface-2 rounded-md flex items-center justify-center text-sm font-mono text-text-secondary">
                  {t.icon}
                </div>
                <div>
                  <div className="text-sm text-text-primary">{t.name}</div>
                  <div className="text-xs text-text-dim">{t.description}</div>
                </div>
              </Command.Item>
            ))}
          </Command.List>
        </div>
      </Command.Dialog>
    </>
  );
}
```

Note: The inner `.map((t) =>` shadows the outer `t` from `useT()`. Rename to `.map((tool) =>` to avoid the shadow:

```tsx
            {tools.map((tool) => (
              <Command.Item
                key={tool.id}
                value={`${tool.name} ${tool.keywords.join(' ')}`}
                onSelect={() => selectTool(tool.id)}
```

- [ ] **Step 3: Run existing tests**

Run: `npx vitest run`
Expected: 49 logic tests still pass (they don't import components)

- [ ] **Step 4: Commit**

```bash
git add src/components/HomePage.tsx src/components/ToolPage.tsx
git commit -m "feat(i18n): translate HomePage and ToolPage strings"
```

---

### Task 7: Replace hardcoded strings in all four Tool.tsx files

**Files:**
- Modify: `src/tools/json/Tool.tsx`
- Modify: `src/tools/timestamp/Tool.tsx`
- Modify: `src/tools/base64/Tool.tsx`
- Modify: `src/tools/url/Tool.tsx`

Each tool follows the same pattern: import `useT`, call `const { t } = useT()`, replace all hardcoded strings with `t('key')` calls.

- [ ] **Step 1: Modify `src/tools/json/Tool.tsx`**

Add import: `import { useT } from '../../i18n/context';`
Add inside component: `const { t } = useT();`

Replace these strings:
- `'Input'` → `t('common.input')`
- `'Output'` → `t('common.output')`
- `{mode === 'format' ? 'Minify' : 'Format'}` → `{mode === 'format' ? t('json.minify') : t('json.format')}`
- `'Clear'` → `t('common.clear')`
- `placeholder="Paste JSON here..."` → `placeholder={t('json.placeholder')}`
- `{copied ? 'Copied!' : 'Copy'}` → `{copied ? t('common.copied') : t('common.copy')}`
- `'Paste your input above to see the result.'` → `t('common.pasteToSeeResult')`
- `title="What does this tool do?"` → `title={t('common.whatDoesThisDo')}`
- The explanation `<p>` text → `{t('json.explanation')}`
- The three `<li>` items → `{t('json.feature1')}`, `{t('json.feature2')}`, `{t('json.feature3')}`

Full replacement:
```tsx
import { useState, useMemo } from 'react';
import { ToolLayout, InputPanel, OutputPanel, Explanation } from '../../components/ToolLayout';
import { formatJSON, minifyJSON } from './logic';
import { useT } from '../../i18n/context';

type Mode = 'format' | 'minify';

export default function JSONTool() {
  const [input, setInput] = useState('');
  const [mode, setMode] = useState<Mode>('format');
  const [copied, setCopied] = useState(false);
  const { t } = useT();

  const result = useMemo(() => {
    return mode === 'format' ? formatJSON(input, t) : minifyJSON(input, t);
  }, [input, mode, t]);

  const error = result.ok ? null : result.error;
  const output = result.ok ? result.formatted : '';

  const copy = async () => {
    if (!output) return;
    try {
      await navigator.clipboard.writeText(output);
      setCopied(true);
      setTimeout(() => setCopied(false), 1500);
    } catch {}
  };

  const clear = () => {
    setInput('');
  };

  return (
    <>
      <ToolLayout error={error}>
        <InputPanel
          label={t('common.input')}
          action={
            <div className="flex gap-2">
              <button
                onClick={() => setMode(mode === 'format' ? 'minify' : 'format')}
                className="text-[11px] px-2 py-1 bg-surface-2 rounded text-text-muted hover:text-text-secondary transition-colors"
              >
                {mode === 'format' ? t('json.minify') : t('json.format')}
              </button>
              <button
                onClick={clear}
                className="text-[11px] px-2 py-1 bg-surface-2 rounded text-text-muted hover:text-text-secondary transition-colors"
              >
                {t('common.clear')}
              </button>
            </div>
          }
        >
          <textarea
            value={input}
            onChange={(e) => setInput(e.target.value)}
            placeholder={t('json.placeholder')}
            className="w-full bg-transparent border-none text-text-secondary text-[13px] leading-relaxed p-4 resize-none min-h-[240px] outline-none font-mono placeholder:text-text-dim"
            spellCheck={false}
          />
        </InputPanel>
        <OutputPanel
          label={t('common.output')}
          action={
            <button
              onClick={copy}
              disabled={!output}
              className={`text-[11px] px-2.5 py-1 rounded transition-colors ${
                copied
                  ? 'bg-green-500/20 text-green-400'
                  : output
                    ? 'bg-accent text-white hover:bg-accent-hover'
                    : 'bg-surface-2 text-text-dim cursor-not-allowed'
              }`}
            >
              {copied ? t('common.copied') : t('common.copy')}
            </button>
          }
        >
          {output ? (
            <pre className="p-4 text-[13px] leading-relaxed min-h-[240px] text-text-secondary whitespace-pre-wrap font-mono">
              {output}
            </pre>
          ) : (
            <div className="p-4 text-[13px] text-text-dim min-h-[240px]">
              {t('common.pasteToSeeResult')}
            </div>
          )}
        </OutputPanel>
      </ToolLayout>
      <Explanation title={t('common.whatDoesThisDo')}>
        <p className="text-[13px] text-text-muted leading-relaxed">
          {t('json.explanation')}
        </p>
        <ul className="mt-2 text-[13px] text-text-muted leading-loose list-disc pl-5">
          <li>{t('json.feature1')}</li>
          <li>{t('json.feature2')}</li>
          <li>{t('json.feature3')}</li>
        </ul>
      </Explanation>
    </>
  );
}
```

- [ ] **Step 2: Modify `src/tools/timestamp/Tool.tsx`**

Same pattern. Add `import { useT } from '../../i18n/context';` and `const { t } = useT();`.

Key replacements:
- `'Input'` → `t('common.input')`
- `'Output'` → `t('common.output')`
- `'Now'` → `t('timestamp.now')`
- `placeholder="Paste a timestamp..."` → `placeholder={t('timestamp.placeholder')}`
- `'Unix Epoch'` → `t('timestamp.unixEpoch')`
- `'ISO 8601'` → `t('timestamp.iso8601')`
- `'RFC 2822'` → `t('timestamp.rfc2822')`
- `'Relative'` → `t('timestamp.relative')`
- `'Paste a timestamp to see all formats.'` → `t('timestamp.pasteToSee')`
- `'Current time:'` → `t('timestamp.currentTime')`
- `{copied ? 'Copied!' : 'Copy'}` → `{copied ? t('common.copied') : t('common.copy')}`
- Explanation title and content → `t('common.whatDoesThisDo')`, `t('timestamp.explanation')`, `t('timestamp.feature1')` etc.

The `parseTimestamp` call needs the `t` function for error messages. Change the `useMemo` to pass `t`:

```tsx
  const result = useMemo(() => {
    if (!input.trim()) return null;
    return parseTimestamp(input, t);
  }, [input, t]);
```

This means `parseTimestamp` in `logic.ts` needs to accept `t` (handled in Task 8).

- [ ] **Step 3: Modify `src/tools/base64/Tool.tsx`**

Same pattern. Key replacements:
- `'Input'` / `'Output'` → `t('common.input')` / `t('common.output')`
- `{mode === 'encode' ? 'Decode' : 'Encode'}` → `{mode === 'encode' ? t('base64.decode') : t('base64.encode')}`
- `placeholder` → `t('base64.encodePlaceholder')` / `t('base64.decodePlaceholder')`
- Empty state → `t('base64.encodeEmpty')` / `t('base64.decodeEmpty')`
- Copy/Copied → `t('common.copy')` / `t('common.copied')`
- Explanation → `t('common.whatDoesThisDo')`, `t('base64.explanation')`, `t('base64.feature1')` etc.

- [ ] **Step 4: Modify `src/tools/url/Tool.tsx`**

Same pattern. Key replacements mirror base64 but with `url.*` keys.

- [ ] **Step 5: Run tests**

Run: `npx vitest run`
Expected: Logic tests still pass (49 tests). Component tests may show type errors if `t` is now required — fix those if they exist.

- [ ] **Step 6: Commit**

```bash
git add src/tools/json/Tool.tsx src/tools/timestamp/Tool.tsx src/tools/base64/Tool.tsx src/tools/url/Tool.tsx
git commit -m "feat(i18n): translate all tool UI strings"
```

---

### Task 8: Localize error messages in logic.ts files

**Files:**
- Modify: `src/tools/json/logic.ts`
- Modify: `src/tools/base64/logic.ts`
- Modify: `src/tools/url/logic.ts`
- Modify: `src/tools/timestamp/logic.ts`

Error messages in logic files are currently hardcoded English. Two approaches:
1. Pass `t` function into logic functions
2. Return error keys instead of messages, translate at the UI layer

**Approach 2 is cleaner** — logic stays pure, UI translates. But the current codebase already returns error strings from logic, and changing the return type would break all tests.

**Practical choice:** Pass `t` into the functions that produce error messages. Update tests to pass a simple identity `t`.

- [ ] **Step 1: Modify `src/tools/json/logic.ts`**

Add a `t` parameter to both functions:

```typescript
export interface FormatResult {
  ok: true;
  formatted: string;
}

export interface FormatError {
  ok: false;
  error: string;
}

export type Result = FormatResult | FormatError;

type TFn = (key: string) => string;

export function formatJSON(input: string, t: TFn = (k) => k): Result {
  if (!input.trim()) {
    return { ok: true, formatted: '' };
  }
  try {
    const parsed = JSON.parse(input);
    return { ok: true, formatted: JSON.stringify(parsed, null, 2) };
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Unknown error';
    const posMatch = msg.match(/position (\d+)/i);
    const pos = posMatch ? t('json.error.atPosition') + posMatch[1] : '';
    return { ok: false, error: t('json.error.invalid') + pos };
  }
}

export function minifyJSON(input: string, t: TFn = (k) => k): Result {
  if (!input.trim()) {
    return { ok: true, formatted: '' };
  }
  try {
    const parsed = JSON.parse(input);
    return { ok: true, formatted: JSON.stringify(parsed) };
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Unknown error';
    const posMatch = msg.match(/position (\d+)/i);
    const pos = posMatch ? t('json.error.atPosition') + posMatch[1] : '';
    return { ok: false, error: t('json.error.invalid') + pos };
  }
}
```

The `t` parameter defaults to identity, so existing tests pass unchanged.

- [ ] **Step 2: Modify `src/tools/base64/logic.ts`**

```typescript
export interface Base64Result {
  ok: true;
  output: string;
}

export interface Base64Error {
  ok: false;
  error: string;
}

export type Result = Base64Result | Base64Error;

type TFn = (key: string) => string;

export function encode(input: string, t: TFn = (k) => k): Result {
  try {
    const encoded = btoa(unescape(encodeURIComponent(input)));
    return { ok: true, output: encoded };
  } catch {
    return { ok: false, error: t('base64.error.encodeFailed') };
  }
}

export function decode(input: string, t: TFn = (k) => k): Result {
  if (!input.trim()) {
    return { ok: true, output: '' };
  }
  try {
    const decoded = decodeURIComponent(escape(atob(input.trim())));
    return { ok: true, output: decoded };
  } catch {
    const match = input.match(/[^A-Za-z0-9+/=]/);
    const pos = match?.index;
    const posMsg = pos !== undefined ? t('base64.error.atPosition') + pos : '';
    return { ok: false, error: t('base64.error.invalid') + posMsg + t('base64.error.period') };
  }
}
```

- [ ] **Step 3: Modify `src/tools/url/logic.ts`**

```typescript
export interface UrlResult {
  ok: true;
  output: string;
}

export interface UrlError {
  ok: false;
  error: string;
}

export type Result = UrlResult | UrlError;

type TFn = (key: string) => string;

export function encode(input: string, t: TFn = (k) => k): Result {
  try {
    const encoded = encodeURIComponent(input);
    return { ok: true, output: encoded };
  } catch {
    return { ok: false, error: t('url.error.encodeFailed') };
  }
}

export function decode(input: string, t: TFn = (k) => k): Result {
  if (!input.trim()) {
    return { ok: true, output: '' };
  }
  try {
    const decoded = decodeURIComponent(input.trim());
    return { ok: true, output: decoded };
  } catch {
    const match = input.match(/%[0-9A-Fa-f]?[^0-9A-Fa-f]/);
    if (match) {
      return {
        ok: false,
        error: t('url.error.invalidWithChar').replace('{char}', match[0]),
      };
    }
    return { ok: false, error: t('url.error.invalidGeneric') };
  }
}
```

- [ ] **Step 4: Modify `src/tools/timestamp/logic.ts`**

The `toRelative` and `parseTimestamp` functions need `t`:

```typescript
export interface TimestampResult {
  unix: number;
  iso: string;
  rfc: string;
  relative: string;
}

export interface TimestampError {
  error: string;
}

export type ParseResult = TimestampResult | TimestampError;

type TFn = (key: string) => string;

function toRelative(date: Date, t: TFn): string {
  const now = Date.now();
  const diff = now - date.getTime();
  const abs = Math.abs(diff);
  const suffix = diff > 0 ? t('timestamp.ago') : t('timestamp.fromNow');

  if (abs < 60_000) return `${Math.floor(abs / 1000)}${t('timestamp.secondsShort')} ${suffix}`;
  if (abs < 3_600_000) return `${Math.floor(abs / 60_000)}${t('timestamp.minutesShort')} ${suffix}`;
  if (abs < 86_400_000) return `${Math.floor(abs / 3_600_000)}${t('timestamp.hoursShort')} ${suffix}`;
  if (abs < 2_629_746_000) return `${Math.floor(abs / 86_400_000)}${t('timestamp.daysShort')} ${suffix}`;
  return `${Math.floor(abs / 2_629_746_000)}${t('timestamp.monthsShort')} ${suffix}`;
}

export function parseTimestamp(input: string, t: TFn = (k) => k): ParseResult {
  const trimmed = input.trim();
  if (!trimmed) return { error: t('timestamp.error.empty') };

  const num = Number(trimmed);
  if (!isNaN(num) && isFinite(num)) {
    const ms = Math.abs(num) > 9999999999 ? num : num * 1000;
    try {
      const date = new Date(ms);
      if (isNaN(date.getTime())) {
        return { error: t('timestamp.error.outOfRange') };
      }
      return {
        unix: Math.floor(ms / 1000),
        iso: date.toISOString(),
        rfc: date.toUTCString(),
        relative: toRelative(date, t),
      };
    } catch {
      return { error: t('timestamp.error.outOfRangeShort') };
    }
  }

  const date = new Date(trimmed);
  if (!isNaN(date.getTime())) {
    return {
      unix: Math.floor(date.getTime() / 1000),
      iso: date.toISOString(),
      rfc: date.toUTCString(),
      relative: toRelative(date, t),
    };
  }

  return {
    error: t('timestamp.error.unrecognized'),
  };
}

export function nowTimestamp(t: TFn = (k) => k): TimestampResult {
  const date = new Date();
  return {
    unix: Math.floor(date.getTime() / 1000),
    iso: date.toISOString(),
    rfc: date.toUTCString(),
    relative: t('timestamp.justNow'),
  };
}
```

Note: `nowTimestamp` now takes `t`. The Timestamp Tool needs to pass it: `nowTimestamp(t)`.

- [ ] **Step 5: Update `src/tools/timestamp/Tool.tsx` — pass `t` to `nowTimestamp`**

Find `useState(nowTimestamp)` and change to:
```tsx
const [current, setCurrent] = useState(() => nowTimestamp(t));
```

And the interval:
```tsx
const id = setInterval(() => setCurrent(nowTimestamp(t)), 1000);
```

But `t` changes identity on locale switch, causing the effect to re-run. Add `t` to the effect's dependency array:
```tsx
useEffect(() => {
  setCurrent(nowTimestamp(t));
  const id = setInterval(() => setCurrent(nowTimestamp(t)), 1000);
  return () => clearInterval(id);
}, [t]);
```

- [ ] **Step 6: Run all tests**

Run: `npx vitest run`
Expected: All 49 tests pass (the default `t = (k) => k` preserves backward compatibility).

- [ ] **Step 7: Commit**

```bash
git add src/tools/json/logic.ts src/tools/base64/logic.ts src/tools/url/logic.ts src/tools/timestamp/logic.ts src/tools/timestamp/Tool.tsx
git commit -m "feat(i18n): localize error messages and relative time in logic files"
```

---

### Task 9: Update index.html title dynamically

**Files:**
- Modify: `index.html`

The HTML `<title>` is static. We set `document.documentElement.lang` in the I18nProvider but should also update `document.title`.

- [ ] **Step 1: Add `document.title` update to `src/i18n/context.tsx`**

Inside the `I18nProvider`, add a `useEffect`:

```tsx
import { createContext, useContext, useState, useCallback, useEffect, type ReactNode } from 'react';
```

Add after `setLocale`:

```tsx
  useEffect(() => {
    document.title = t('app.title');
    document.documentElement.lang = locale === 'zh' ? 'zh-CN' : 'en';
  }, [locale, t]);
```

- [ ] **Step 2: Verify in browser**

Run: `npm run dev`
Open browser. Title should show "Raccoon — 开发者工具箱" by default. Switch to English → title changes to "Raccoon — Developer Toolbox".

- [ ] **Step 3: Commit**

```bash
git add src/i18n/context.tsx
git commit -m "feat(i18n): dynamically update page title on locale change"
```

---

### Task 10: Final integration test and cleanup

**Files:**
- Verify all files compile and tests pass

- [ ] **Step 1: Run full test suite**

Run: `npx vitest run`
Expected: 49 logic tests + 4 i18n context tests = 53 tests pass.

- [ ] **Step 2: Run TypeScript check**

Run: `npx tsc --noEmit`
Expected: No errors.

- [ ] **Step 3: Run production build**

Run: `npm run build`
Expected: Build succeeds.

- [ ] **Step 4: Manual browser verification**

Run: `npm run dev`
Check:
1. Default language is Chinese — all strings show Chinese
2. Click language toggle → switches to English
3. Refresh page → stays in last selected language (localStorage)
4. All 4 tools work correctly in both languages
5. Error messages appear in the correct language (paste invalid JSON, invalid Base64, etc.)
6. Relative time shows Chinese units ("5分钟 前") in Chinese mode

- [ ] **Step 5: Commit any remaining fixes**

```bash
git add -A
git commit -m "fix(i18n): integration fixes after full verification"
```
